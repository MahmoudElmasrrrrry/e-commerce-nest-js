export enum Role {
  User = 'user',
  Admin = 'admin',
}

export enum Sex{
    Male = 'male', 
    Female = 'female'
}


export enum EmailType {
    VERIFY_EMAIL = "VERIFY_EMAIL",
    RESET_PASSWORD = "RESET_PASSWORD",
}